# AGENTS.md — 에이전트 작업 지도

이 저장소를 다루는 AI 에이전트(헤르메스·Codex)를 위한 안내입니다.

## 무엇 하는 저장소인가

MAGMA의 데이터 분석 사업부 작업 공간입니다. 시장 데이터의 수집, 적재, 정제, 분석, 대시보드 제작이 여기서 이루어집니다. 회사소개 사이트(magma-content-site)는 별도 저장소이며, 완성된 보고서를 게재할 때만 그쪽 발행 API를 사용합니다.

## 폴더 규약

| 폴더 | 용도 | 규칙 |
| --- | --- | --- |
| `supabase/` | 마이그레이션 파일 | 스키마 변경은 반드시 마이그레이션 파일로 남긴다. 대시보드 SQL 편집기로 임의 변경하지 않는다 |
| `data/collect/` | 수집 CSV | 파일명에 수집일을 포함한다 (예: `collected_products_YYYY-MM-DD.csv`) |
| `data/mart/` | 실습용 판매 데이터 | 생성기 출력물을 그대로 둔다. 정제 결과는 별도 파일로 저장한다 |
| `dashboard/` | 대시보드 앱 | 앱 생성 시 이 폴더 하나에 자족적으로 만든다 |

## 작업 원칙

1. **승인 전 적용 금지.** 스키마 설계안·마이그레이션은 먼저 안을 올리고, 사람이 승인한 뒤에만 적용 단계로 넘어간다.
2. **데이터베이스 구조 변경은 마이그레이션으로만.** 적용 이력과 실제 상태가 어긋나지 않게 유지한다.
3. **수집 윤리.** 공개 화면의 집계 수치만 수집한다. 리뷰 본문 등 저작물과 개인정보성 데이터는 수집하지 않는다. 같은 사이트에 과도한 반복 요청을 보내지 않는다.
4. **시크릿 비노출.** 토큰·비밀번호를 파일이나 출력에 남기지 않는다.
5. 파일을 지우거나 덮어쓰는 작업은 사람에게 먼저 확인받는다.

## 수집·적재 파이프라인 규칙

여러 담당자가 카드로 이어 일할 때 지킨다.

**적재 대상**: 수집한 상품 데이터는 이 저장소의 Supabase 프로젝트에서 **데이터베이스 준비 실습(7.1) 때 만든 수집 테이블**에 넣는다. 테이블 이름은 환경마다 다를 수 있으니 적재 전에 실제 표 구조를 조회해 확인한다. 적재는 append-only — 기존 행을 갱신하거나 덮어쓰지 않는다.

1. **파일은 절대경로로 주고받는다.** 상대경로로 넘기지 않는다.
2. **작업을 마치면 완료 요약에 산출물의 절대경로와 건수를 적는다.** 다음 담당자는 이 요약만 보고 이어받는다.
3. **없는 값을 만들어 채우지 않는다.** 필수 항목이 비어 넣을 수 없는 행은 제외하고, 제외 건수와 사유를 보고한다. 추측으로 주소·이름·가격을 지어내지 않는다.
4. **표에 대응 칸이 없는 값은 버리지 말고 담되, 이름은 원본 그대로 쓴다.** 새로 지어내지 않는다.
5. **적재 전에 실제 표 구조를 조회해 맞춘다.** 파일과 표의 칸 이름이 다를 수 있다.
6. **막히면 임의로 대체 수단을 만들지 말고**(예: 다른 곳에 임시 저장) 무엇이 막혔는지 보고한다.
