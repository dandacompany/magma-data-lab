# PROMPTS — 에이전트에게 보내는 메시지 모음

데이터 분석 사업부 실습에서 에이전트에게 보내는 자연어 메시지를 순서대로 모았습니다. 유닛별로 나눠 두었고, 각 항목에 누구에게 보내는지 적어 두었습니다.
코드 블록을 통째로 복사해 에이다 대화창에 붙여넣으세요. 괄호 자리만 본인 상황에 맞게 바꿉니다.

# 데이터베이스 준비 실습 (7.1)

## 1. 장비 점검 (새 세션 시작 직후, 기안 요청 전에)

```text
새 사업부에서 일할 준비가 됐는지 네 장비를 점검해줘.
1. 수파베이스 연결로 우리 프로젝트가 조회되는지
2. 수파베이스 공식 스킬이 로드돼 있는지
3. 지금 작업 폴더가 어디고, 이 저장소의 작업 규칙이 무엇인지
확인해서 요약해줘. 안 되는 게 있으면 뭐가 안 되는지만 알려주고 고치려고 하지는 마.
```

## 2. 스키마 기안 요청

```text
데이터 분석 사업부의 첫 데이터베이스 스키마를 기안해줘.

배경: 다음 시간부터 무신사, 네이버 가격비교, 쿠팡 세 곳에서 남성 패션 상품 데이터를
수집해서 이 데이터베이스에 쌓을 거야. 소스마다 주는 정보가 달라. 무신사는 브랜드와
평점, 리뷰 수를 주고, 네이버는 평점, 리뷰 수에 구매 수와 상품 속성까지 주는데,
쿠팡은 리뷰 수는 주지만 평점을 안 줘.

요구사항:
1. 수집한 상품 데이터를 담을 테이블을 설계한다. 소스마다 없는 값이 있어도 깨지지 않아야 한다.
2. 어떤 상품이 어느 사이트에서 언제 수집됐는지 추적할 수 있어야 한다.
3. 같은 상품을 여러 번 수집해도 이전 기록이 덮이지 않고 남아야 한다.

먼저 통합 테이블 하나로 제안하되, 소스별로 나누는 게 더 낫다고 판단하면 그 대안도 함께 설명해줘.
형식: 테이블 이름, 컬럼 이름과 타입, 각 컬럼이 왜 필요한지 한 줄 설명.
먼저 설계안만 올려줘. 내가 검토하고 승인하기 전에는 데이터베이스에 적용하지 마.
```

마지막 두 문장이 핵심입니다. 이 문장 패턴만 가져가면 어떤 도구를 쓰든
"에이전트가 임의로 실행하지 않는" 안전장치가 재현됩니다.
본인 업무에 쓸 때는 배경과 요구사항만 바꾸세요.

## 3. 승인과 마이그레이션 지시 (설계안 검토·수정을 마친 뒤)

```text
승인한다. 이 설계를 마이그레이션 파일로 만들어줘. 적용 명령은 내가 직접 실행할게.
```

## 4. 최종 확인 (마이그레이션 적용 후)

```text
지금 데이터베이스에 어떤 테이블이 있는지 조회해서 구조를 요약해줘.
```

---

# 데이터 수집·적재 실습 (7.2)

## 5. 손으로 한 번 확인 (Oliver)

```text
무신사에서 남성 셔츠 검색 결과 첫 페이지를 그대로 가져와서 보여줘.
상품명, 브랜드, 가격, 평점, 리뷰 수가 보이는지 확인만 하면 돼.
```

같은 요청을 29cm에도 해 보세요. 상품이 나오지 않는 것이 정상입니다.

## 6. 보드와 카드 만들기 (Oliver)

```text
칸반 작업을 준비해줘. 터미널 도구로 hermes kanban 명령을 실행하면 돼.

1. magma-data 라는 보드를 만든다. 표시 이름은 '데이터 분석 사업부',
   기본 작업 폴더는 ~/.hermes/workspace/magma-data-lab 로 한다.
2. 그 보드에 카드 두 장을 만든다. 둘 다 작업 폴더는 위와 같다.
   - 첫 번째: 제목 '커머스 3사 상품 수집', 담당 oliver
     본문: 무신사·네이버·쿠팡에서 남성셔츠 상위 10개씩 수집해 정규 CSV로 data/collect 아래에 저장한다.
           magma-collect 스킬의 형식과 검증기를 따른다.
   - 두 번째: 제목 '수집 CSV 검증·적재', 담당 ada, 첫 번째 카드를 부모로 연결
     본문: 부모가 넘긴 수집 CSV를 검증하고 데이터베이스에 적재한다.
3. 만든 뒤 첫 번째 카드에 block 명령을 걸어 차단 상태로 둬라.
   내가 시작할 때까지 실행되면 안 된다.
4. 보드의 카드 목록을 보여줘.

확인 물음이 나오면 전부 Y로 진행해.
```

## 7. 시작 (터미널)

카드 번호는 위에서 보고받은 목록에 있습니다.

```bash
hermes kanban --board magma-data unblock (수집 카드 번호)
```

## 8. 결과 확인 (Ada)

```text
지금 데이터베이스에 몇 행이 들어갔는지, 사이트별로 몇 건인지 조회해서 알려줘.
```

---

# 데이터 정제·분석 실습 (7.3)

## 8-2. Sam에게 도구 붙이기 (터미널)

Sam은 데이터베이스를 만져본 적이 없습니다. 7.1에서 연결과 스킬을 붙인 건 Ada였고, **프로필이 다르면 도구도 따로**입니다. 7.1에서 Ada에게 쓴 같은 명령을 프로필 이름만 바꿔 실행합니다.

```bash
hermes -p sam mcp add supabase --url "https://mcp.supabase.com/mcp" --auth header
```

```bash
hermes -p sam skills install skills-sh/supabase/agent-skills/supabase
```

`29/29 tools enabled`가 뜨면 성공입니다.

주소가 Ada 것보다 짧습니다. Ada에게는 프로젝트 아이디를 박아 **그 프로젝트만** 보게 했고(도구 20개), Sam은 개발자라 계정 전체를 봅니다(도구 29개 — 프로젝트 생성·정지·비용 확인이 추가됩니다). **역할이 다르면 권한도 다릅니다.**

토큰은 Ada에게 쓴 것과 같은 값을 넣되 **입력은 따로** 해야 합니다. 프로필마다 인증을 따로 보관합니다.

⚠️ "Ada 설정을 복제해줘"라고 에이전트에게 시키면 설정과 스킬은 복제되지만 **인증이 따라오지 않아** 실제 조회에서 권한 오류가 납니다. 그런데도 완료했다고 보고합니다. 두 줄 직접 실행이 확실합니다.

## 9. 마트 생성 스킬 설치 (터미널)

7.2에서 수집 스킬을 가져왔던 저장소(`github.com/dandacompany/dante-skills`)에 있습니다. 이번엔 **ada** 프로필에 답니다.

```bash
hermes -p ada skills install dandacompany/dante-skills/magma-mart-seed
```

⚠️ `hermes skills search`로는 찾을 수 없습니다. 이 저장소의 스킬들은 검색 목록에 올라가 있지 않아서 위처럼 **저장소 경로를 직접 적어야** 설치됩니다. 검색해서 안 나온다고 잘못된 게 아닙니다.

## 10. 판매 마트 생성 — 아직 넣지 않는다 (Ada)

```text
magma-mart-seed 스킬로 판매 데이터 마트를 만들어줘. 아직 데이터베이스에 넣지는 마.
만들고 나면 표별 행 수와 어떤 오염이 얼마나 들어갔는지 요약해서 보여줘.
```

같이 나오는 `DIRT_REPORT.md`가 오늘 찾아낼 것들의 정답지입니다. 훑어보고 덮어두세요.

에이다가 정답지 숫자를 그대로 옮기지 않고 **만들어진 CSV를 다시 세어** 보고할 수 있습니다. 숫자가 조금씩 달라도 정상입니다(중복으로 복제된 행이 오염도 함께 복제하기 때문). 파일은 `data/mart/` 아래 날짜가 붙은 폴더로 생깁니다.

## 11. 적재 승인 (Ada)

```text
좋아, 이제 우리 수파베이스 데이터베이스에 적재해줘.

행이 5만 건이 넘으니까 한 건씩 넣지 말고 배치로 묶는 스크립트를 짜서 실행해줘.
적재가 끝나면 테이블별 행 수를 조회해서 CSV 행 수와 맞는지 확인해줘.
```

기대 결과: customers 1,030 · products 200 · promotions 30 · orders 20,060 · order_items 33,370 (합계 54,690행)

7.1·7.2에서 만든 시장 데이터 표는 지우지 않습니다. 그 옆에 다섯 개가 추가되는 것이고, 다음 유닛에서 둘 다 씁니다.

⚠️ 적재를 두 번 하면 행이 두 배가 됩니다. 표 생성 SQL이 `create table if not exists`라 데이터는 지워지지 않습니다. 처음부터 다시 하실 거면 적재 전에 비우세요.

```sql
truncate stg_customers, stg_products, stg_promotions, stg_orders, stg_order_items;
```

## 12. 채널별 주문 건수 — 먼저 평범하게 (Ada)

```text
지금 데이터로 채널별 주문 건수를 뽑아줘. 어느 채널의 주문 건수가 제일 많아?
```

이 답의 숫자는 **물을 때마다 달라집니다.** 에이다가 채널 이름을 어디까지 묶을지, 취소된 주문을 뺄지를 그때그때 스스로 판단하기 때문입니다. 숫자를 외우지 말고 넘어가세요.

## 13. 같은 질문, 방식만 바꿔서 (Ada)

에이다가 채널 이름을 알아서 묶어서 셉니다. 적힌 그대로 세면 답이 달라집니다.

```text
방금 건 네가 채널 이름을 정리해서 센 거지?
정리하지 말고 데이터에 적힌 값 그대로, 표기별로 세면 어떻게 나와?
```

답이 두 개 나오는 것이 정상입니다. 둘 다 계산은 맞습니다. 무엇을 하나로 볼지 정하지 않았을 뿐입니다.

## 14. 오염 진단 (Ada)

```text
스테이징 테이블 다섯 개를 조사해서 데이터 품질 문제를 찾아줘.

실제로 조회해서 확인한 것만 보고해. 문제 종류별로 무엇이·몇 건·왜 문제인지를 표로 정리하고,
그중 처리 방법을 내가 판단해야 하는 것은 따로 표시해줘.

아직 아무것도 고치지 마. 진단만.
```

마지막 줄이 중요합니다. 고치는 건 기준을 정한 다음 일입니다.

⚠️ **판단이 필요한 항목이 열 개 넘게 나옵니다**(실측 14종). 실제 업무도 이렇습니다. 다 처리하려 들지 마세요. **매출 숫자를 직접 흔드는 것만 골라** 다음 단계로 갑니다. 강의에서 고른 일곱 가지는 16번 프롬프트에 그대로 들어 있습니다.

## 15. 손실액 확인 — 버릴지 정하기 전에 (Ada)

```text
상품 테이블에 없는 상품을 가리키는 주문 항목이 몇 건이고, 그 금액이 전체 매출의 몇 퍼센트인지 알려줘.
```

강의 실측: 260행 · 4,678만 원 · 전체의 0.42%. 이 정도면 빼도 전체 그림이 안 바뀌지만, **5%였다면 다른 결정**을 했을 겁니다. 숫자를 보고 정하는 게 요점입니다.

## 16. 정제 뷰 생성 (Sam)

아래 일곱 가지는 진단에서 나온 항목 중 **매출을 흔드는 것만 고른 것**입니다. 앞의 넷은 정답이 정해져 있고, 뒤의 셋(5·6·7번)이 여러분이 내린 결정입니다. 다르게 판단했다면 바꿔서 보내세요.

첫 줄의 "수파베이스 연결(MCP 도구)로"를 빼면 샘이 셸에서 접속 정보를 찾다가 멈춥니다. 6번 날짜도 고정으로 두세요 — "오늘 이후"로 바꾸면 보는 시점에 따라 결과가 달라집니다.

```text
수파베이스 연결(MCP 도구)로 작업해줘. 에이다가 진단한 문제에 대해 내가 처리 기준을 정했어.
이 기준대로 정제 뷰를 만들어줘. 원본 테이블은 절대 바꾸지 마. 뷰만 만든다.

1. 채널 표기 통일 — 대소문자·공백 무시, 한글도 영문으로. online, store, pop-up 세 가지로.
2. 주문일을 날짜 타입으로 통일.
3. 완전 중복 주문 행은 하나만.
4. 단가를 숫자로 통일.
5. 수량 음수인 주문 항목 제외.
6. 분석 기준일은 2026-06-30이다. 그 이후 주문은 제외. 현재 날짜를 쓰지 말고 이 날짜를 고정으로 써라.
7. 상품 테이블에 없는 상품을 참조하는 주문 항목 제외.

형식을 바꾸는 2번과 4번은 이렇게 해줘.
- 실제로 어떤 형식들이 들어 있는지 **먼저 조회해서 확인**하고, 확인한 형식을 전부 처리해라. 추측해서 만들지 마.
- 변환에 실패해서 값이 비는 행이 몇 건인지 세어봐라. **0이 아니면 뷰를 만들지 말고 멈춰서 보고**해라.

뷰 이름: v_clean_orders, v_clean_order_items

만든 뒤 **원본 대비 몇 건이 남았고 어떤 이유로 몇 건이 빠졌는지 표로** 보고해줘.
그다음 채널별 주문 건수를 조회해 보여줘.
```

## 17. 같은 질문 다시 (Ada)

```text
정제 뷰 v_clean_orders로 채널별 주문 건수를 다시 뽑아줘. 어느 채널의 주문 건수가 제일 많아?
```

이 답이 기준을 정하고 나온 답입니다. 앞의 두 답과 달리 왜 이 숫자인지 설명할 수 있습니다.

## 18. 분석 질문 (Ada)

첫 줄이 핵심입니다. 취소된 주문을 매출에 넣을지는 데이터가 아니라 업무가 정합니다.

```text
정제 뷰로 분석해줘. 매출은 결제가 완료된 주문만 세는 걸로 하자.

1. 채널별 매출이 얼마야? 건수 1위와 매출 1위가 같아?
2. 월별 매출 추이가 어때? 가장 높은 달은?
3. 채널별 평균 주문금액에 차이가 있어?

SQL도 같이 보여줘.
```

# BI 대시보드 제작 실습 (7.4)

## 19. 첫 차트 요청 — 경계를 그어서 (Sam)

세 번째 줄이 오늘의 첫 학습 포인트입니다. **이 줄이 없으면 에이전트는 멈추지 않습니다.** 아직 닦지 않은 원본을 알아서 이어 붙여 그럴듯한 답을 만들어 냅니다. 숫자는 정제 기준과 다른데 화면에는 아무 경고도 안 뜹니다.

```text
우리 회사 판매 대시보드에 지역별 매출 차트를 넣으려고 해.
앞에서 만든 정제 뷰 v_clean_orders 와 v_clean_order_items 만 사용해서 지역별 매출을 집계해 줘.
정제 뷰에 없는 컬럼이 필요하면 원본 테이블을 임의로 조인하지 말고, 무엇이 부족한지 멈춰서 보고해.
수파베이스 MCP를 사용해.
```

Sam이 "중단했습니다"로 답하며 두 뷰의 컬럼을 나열하고, 지역을 나눌 기준이 없다고 보고하면 정상입니다. 정제는 한 번으로 끝나지 않습니다. 새 질문이 생기면 그때 또 합니다.

## 20. 오염 진단 — 범위를 좁혀서 (Ada)

마지막 줄도 지우지 마세요. **범위를 안 정해 주면 이메일·전화번호·가입일까지 전부 훑느라 몇 분이 걸립니다.** 19번이 "없는 걸 지어내지 마라"는 경계였다면, 이건 "묻지 않은 걸 파고들지 마라"는 경계입니다. 둘 다 사람이 긋는 것입니다.

```text
대시보드를 만들려는데 정제 뷰에 지역과 카테고리가 없어.
수파베이스 MCP로 stg_customers 의 지역과 stg_products 의 카테고리를 진단해 줘.
어떤 값이 몇 건인지 표로 보고해. 아직 고치지는 마.
지금 필요한 건 이 두 가지야. 가격이나 다른 컬럼은 지금 보지 마.
```

지역 표기가 표준 시도명 외에 53건, 카테고리는 빈 값 5건이 나옵니다. 지역 표에서 같은 이름이 두 줄로 나뉘어 보이면 하나는 앞에 공백이 붙은 것입니다. 눈으로는 안 보이는 오염입니다.

## 21. 정제 뷰 세 개 생성 (Ada)

기준은 여러분이 정합니다. 아래는 예시이고, 카테고리 빈 값을 버릴지 살릴지, 취소·환불 주문을 남길지는 업무가 정하는 것입니다.

```text
방금 정한 기준대로 수파베이스 MCP로 세 개를 만들어 줘.
1. v_clean_customers - 지역은 시도명으로 통일, 카테고리 빈값은 처리하지 말고 고객만. 성별은 M/F/미상.
2. v_clean_products - 카테고리 빈값은 '미분류'로 채우고, 원가나 판매가가 0 이하인 상품은 제외.
3. v_sales - 정제된 주문·주문상품·고객·상품을 이어 붙인 판매 팩트 뷰. 주문일시에서 날짜·월·요일·시간대를 미리 계산해 넣고, 매출액과 마진도 계산해 줘.
지켜야 할 것.
- 실제로 어떤 값들이 들어 있는지 먼저 조회해서 확인하고 처리해라. 추측해서 만들지 마.
- 원본 테이블은 건드리지 말고 전부 뷰로만 만들어라.
- 만들기 전에 SQL을 보여주고 내 승인을 받아라.
- 만든 뒤 원본 대비 몇 건이 남았고 어떤 이유로 몇 건이 빠졌는지 표로 보고해라.
```

7.3의 방어 세 줄이 그대로 들어갔습니다. 먼저 조회하고, 승인받고, 잔존을 보고하게 하는 것입니다.

## 22. 승인 전에 SQL을 읽고 반려 (Ada)

**승인 버튼을 누르기 전에 SQL을 직접 읽으세요.** 정하지 않은 필터가 들어가 있는 경우가 있습니다. 예를 들어 마지막에 `where status = 'paid'` 가 붙어 있으면 취소·환불 주문이 통째로 사라집니다. 화면에는 경고가 안 뜹니다.

```text
잠깐. v_sales 마지막에 status = paid 조건이 붙어 있는데, 나는 취소와 환불 주문도 남기라고 했어.
상태 컬럼으로 구분만 되게 두고 그 필터는 빼 줘. 매출을 결제 완료로 좁히는 건 나중에 보고서에서 할 거야.
그 부분만 고쳐서 다시 보여줘.
```

SQL이 기준대로면 그냥 승인하면 됩니다. 이 메시지는 어긋났을 때만 씁니다.

## 23. Evidence 프로젝트 만들기 (터미널)

```bash
cd ~/.hermes/workspace/magma-data-lab
npx degit evidence-dev/template dashboard --force
cd dashboard
npm install
```

`--force` 는 `dashboard` 폴더에 들어 있는 안내용 파일을 덮어쓰기 위한 것입니다. 빼면 `destination directory is not empty` 로 중단됩니다. 원래 여기 채우라고 만들어 둔 자리입니다.

## 24. CA 인증서 받기 (터미널)

```bash
curl -sL "https://supabase-downloads.s3.ap-southeast-1.amazonaws.com/prod/ssl/prod-ca-2021.crt" -o prod-ca-2021.crt
```

⚠️ 인터넷에는 "인증서 검사를 끄면 된다"(`rejectUnauthorized: false`)는 방법이 많이 돌아다닙니다. **쓰지 마세요.** 중간에서 통신을 가로채는 공격을 막는 장치를 끄는 것입니다. 정식 경로는 이렇게 인증서 한 장을 받는 것입니다.

## 25. 접속 정보 넣기 — `dashboard/.env`

```text
EVIDENCE_SOURCE__magma__user=postgres.내프로젝트ref
EVIDENCE_SOURCE__magma__password=내비밀번호
```

사용자 이름은 Supabase 대시보드의 **Connect** 화면에서 복사합니다. 비밀번호는 7.1에서 데이터베이스를 만들 때 정한 그 비밀번호입니다.

저장한 뒤 깃에 올라가지 않는지 확인하세요. 출력이 나오면 무시되고 있는 것입니다.

```bash
git check-ignore -v .env
```

## 26. 대시보드 스킬 설치 (터미널)

```bash
hermes -p sam skills install dandacompany/dante-skills/magma-dashboard
```

설치 전에 어느 저장소의 어느 커밋인지, 언제 검사했는지가 표시되고 **제3자 스킬은 본인 책임**이라는 경고가 나옵니다. 읽고 `y` 를 누릅니다. 누르는 건 사람입니다.

## 27. 대시보드 제작 위임 (Sam)

마지막 두 줄이 21번에서 이미 정한 기준인데 또 나옵니다. 그건 **Ada와 나눈 대화**이고 Sam은 그 대화를 못 봤기 때문입니다. **프로필이 다르면 각자 자기 기억만 갖고 있습니다.** 한 번 정한 기준은 다음 위임에 다시 적어야 합니다.

```text
magma-dashboard 스킬을 사용해서 우리 회사 판매 대시보드를 만들어 줘.
Evidence 프로젝트는 ~/.hermes/workspace/magma-data-lab/dashboard 에 준비돼 있고,
접속 정보가 담긴 .env 와 CA 인증서 prod-ca-2021.crt 도 프로젝트 루트에 있어.
스킬 절차대로 소스 연결부터 페이지 작성, 그리고 검산 보고까지 진행해.
데이터 원천은 수파베이스의 v_sales 뷰야.
매출은 취소와 환불까지 포함한 전체 주문 기준으로 집계하고, 주문 상태로 구분만 되게 해 줘.
결제 완료로 좁히는 건 다음에 보고서에서 할 거야.
```

Sam이 만들기만 하는 게 아니라 **검산 대조표**를 함께 냅니다. 화면 숫자와 원본 집계를 대조한 표입니다. 전부 일치하면 "뷰를 정확히 옮겼다"는 뜻이지, "뷰의 숫자가 옳다"는 뜻은 아닙니다.

## 28. 화면 띄우기 (터미널)

```bash
NODE_EXTRA_CA_CERTS=./prod-ca-2021.crt npm run dev
```

터미널에 표시된 **Local 주소**를 브라우저에서 엽니다. 포트는 3000이 아닐 수 있습니다.

## 29. 이상한 숫자의 원인 조사 (Ada)

상품별 실적 표에서 마진율이 유독 높은 상품이 보이면 그 이름을 넣어 물어봅니다. **고치지 말고 원인만** 이라고 적는 이유가 있습니다. 원인을 보고 어떻게 할지는 여러분이 정할 일입니다.

```text
대시보드의 상품별 실적 표를 보는데 이상한 게 있어.
시티 카라 티셔츠, 오버핏 치노 팬츠, 프리미엄 카라 티셔츠 세 상품만 마진율이 99% 근처이고
매출도 다른 상품보다 자릿수가 커.
수파베이스 MCP로 v_sales 뷰와 원본 테이블을 조사해서 왜 이 세 상품만 이런 값이 나오는지 원인을 찾아 줘.
데이터를 고치지는 말고 원인만 보고해.
```

세 상품의 판매가가 원본부터 백 배쯤 크게 저장돼 있다는 진단이 나옵니다. 다만 **진짜 의도한 가격이 얼마였는지는 데이터베이스만 봐서는 알 수 없습니다.** 여기서 판단이 사람에게 넘어옵니다.

## 30. 정한 기준을 반영시키기 (Sam)

확인되지 않은 값은 **추측으로 고치지 않습니다.** 임시로 빼두고, 뺐다는 사실을 남깁니다.

```text
정제 기준을 두 가지 추가할게.
1. 상품 P0022, P0044, P0079 의 단가는 백 배쯤 커 보이는 미확인 이상값이야. 진짜 가격을 확인하기 전까지 v_sales 에서 임시 제외하고, 제외 사유를 뷰 주석에 남겨 줘.
2. v_sales 에 product_id 컬럼을 추가해 줘. 같은 이름의 다른 상품이 합산되지 않게.
반영한 뒤에 대시보드 데이터를 다시 적재하고, 검산 대조표도 다시 보고해 줘.
```

⚠️ **반영하고 나면 총매출이 크게 떨어집니다.** 행은 1.5%밖에 안 빠지는데 매출은 절반 넘게 줄어듭니다. 잘못된 게 아닙니다. 확인 안 된 값 세 개가 매출 그림을 지배하고 있었다는 뜻입니다.

**그래서 제외 사유를 반드시 남겨야 합니다.** 설명 없이 이 화면을 남에게 보여주면 매출을 절반으로 잘못 보고하는 셈이 됩니다.

## 31. 빌드하고 배포하기 (터미널)

```bash
NODE_EXTRA_CA_CERTS=./prod-ca-2021.crt npm run build
cd build && vercel deploy . --prod --yes --name magma-dashboard
```

`--name` 을 빼면 폴더 이름인 `build` 가 프로젝트 이름이 됩니다. `vercel` 명령이 없으면 먼저 `npm i -g vercel` 로 설치하고 로그인합니다.

## 32. 공개 설정 풀기 — 이 단계를 빼면 남이 못 봅니다

새로 만든 Vercel 프로젝트는 **기본으로 잠겨 있습니다.** 본인 브라우저에서는 잘 보이기 때문에 알아채기 어렵습니다.

Vercel 대시보드 → 해당 프로젝트 → **Settings → Deployment Protection → Vercel Authentication** 을 끕니다.

확인은 **시크릿 창**에서 합니다. 로그인 없이 열리면 성공입니다.

⚠️ 주소는 **배포 출력에 찍힌 것을 그대로** 복사하세요. `magma-dashboard.vercel.app` 처럼 짧은 주소를 프로젝트 이름으로 추측해 만들면 **남이 이미 쓰고 있는 사이트**가 열릴 수 있습니다.
